const Digitos = require('./digitos')

// hacer una funcion ternaria
console.log(Digitos.Validation(555) ? 'Sí cumple, el número tiene tres digitos' : 'No cumple, el número tiene una cantidad diferente a tres digitos')
